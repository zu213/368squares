name: Update submodule in zupstn-website

on:
  push:
    branches: [main]

jobs:
  update-submodule:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: zu213/zupstn-website
          token: ${{ secrets.SUBMODULE_PAT }}
          submodules: true

      - name: Update submodule
        run: |
          git submodule update --remote public/squares
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/squares
          git diff --staged --quiet || git commit -m "chore: update 368squares submodule"
          git push
